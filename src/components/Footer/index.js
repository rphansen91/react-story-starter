import React from "react";
import ServerStatus from "../Server";

export default () => (
  <footer className="jumbotron rounded-0 mb-0 w-100">
    <div className="container">
      <ServerStatus />
    </div>
  </footer>
);
